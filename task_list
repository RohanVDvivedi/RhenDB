PICKED TASKS


* SQL_INT allowed with 1 to 8 bytes, SQL_BIGINT allowed with 1 to 32 bytes, 1 more spec bit suggests if it is signed, passing it as 0 means unsigned, 1 means signed





CATALOG MANAGER
* implement it's read and write functions










* constant folding to be done in sqltoast, by storing a pointer at every expression node, which is non NULL if the expression evaluates to a constant










PRIORITY 1 SIMPLE OPERATORS SOURCE/SINK

DB acess operators
 * scan over relation_heap, in parallel with all partitions, atmost max_concurrent scans at a time (root_page_id, tuple_pointer, tuple_with_mvcc_versions) -> tuple_pointer can alone uniquely identify it, but we need root_page_id to update/delete it, it will have 1-7 output modes
 * insert/update/delete into relation for sink operator
 * scan over relation_index (bplus_tree index), in parallel with all partitions, given start_scan_key, start_find_pos and stop_scan_key, stop_find_pos, decides scan direction on it's own, start and scan can be exclusive inclusive, outputs same 1-7 output modes based tuples, i.e. looks up heap tuples on it's own
 * index_nested_loop_join
 * index_nested_loop_semi_join
   * both joins take key_element_count as input, and the index to look up from

 * mvcc check is done for the output of every scan
 * filter check is done again for the output of index if it's key is possibly extended type

 * deletes taken care of by trash_cleaner task over that tuple, to be implemented later, also takes care of filling up the hint bits of mvcc headers

 * table_locks -> locks the table_id of the table for schema changes
 * tuple_locks -> lock root_page_id, page_id, tuple_index -> on update or delete, lock the older version, newer version is not even visible
 * key_locks -> hash key how ever big it is concatenate it with root_page_id and lock it

 * for auto_increment store a counter in the db_table object and increment it and return it
 * for unique constraint maintain a lock in lock_manager and hold lock on the 80-bit serialized hash value and then only check the unique btree index







PRIORITY HIGH LEVEL TASKS (not yet prioritized)

* aggregate functions
 * group_concat -> canctaenate all strings in same group, into a volatile string
 * gcd -> same return type for the number (ponder over support for lcm)
 * bitwise_and/or/xor -> only integer types wiuth same number of bytes/bits in them
 * logical_and/or/xor -> single bit number, doen after type casting to bool

* functions
* need preloaded functions and mechanism to call them like length of string/array/blob/binary, trigonometery, max, min with variable lengths inputs
 * implement function NOW(), to get current epoch
 * implement a function to get i-th element from an array, tuple, string, blob, binary
 * implement a function to convert a byte to it's ascii value, can also allow type cast to do it
 * implement get date, time, with timezone, and time in string to unix epoch
 * implement concat and substring
 * implement double/numeric funtions, sin, cos, tan, asin, acos, atan, log, expr

FUTURE TASKS

 2.* maintain the inserts and deletes log as a stack in volatile page store and allow save points (*future)
   * savepoints will flip the is_xmin_NULL bit to 1 to undo an insert
   * and will flip the is_xmax_NULL bit to 1 to undo a delete
   * then we will do this until we hit a ROLLBACK to savepoint is hit
   * we will let trash_cleaner do it's task of cleaning up inserts later in time
   * this stack will be in volatile page store and upon a crash we will just consider this transaction as aborted and we will never need this information

 3.* define statistics tables (*future and (low priority)) - persistent
   * same structurally and composed of indexes and heap_tables

 4.* lock table remaining tasks (*future)
   * deadlock detection and resolution task job (*future, for now rely on timeouts to back off and abort)
   * define basic lock martix for row, heap_table (ordering_lock), simple reader writer lock (*future, do it when query optimizer and catalog tables are ready)

 5.* always sum up all the operator_resource_counter-s after making the operators and request that many resources from the rhendb's resource_usage_limiter-s
   * once the query is done and shutdown, free up all the resources
   * if required store the buffers_allotted and thread_alloted in the query plan, and free the resources when not needed by some means later
   * finally before we do all this, make sure that the operator actually calculates the right number of resources for bufferpool and threadpool

DESIGN METHODOLOGY
   * we will use postgresql architecture over (mysql like) mini transactions
   * we will have heap_table for storing tables, insert to which gives us a page_id (physical page_id) and a tuple_index (slot no) in that page
   * indexes will always include the page_id, tuple_index in the index key right after the actual key columns, and the index entry will be index key followed by the covering columns
   * inserts to table
     * insert row in heap_table
     * insert all index entries in all indexes, only after heap table row is inserted
   * true deletion only happens in trash_cleaner job (aka postgresql's vaccum)
     * scan heap_table and find the rows on a page that need to be discarded
     * discard all the index entries for these row
     * only then discard the heap_table row
   * This above ordering ensures that we will never find an index entry that has a dangling heap tuple pointer
     * i.e. on following an index entry, we will always find something
   * inserts/deletes will always be done to heap in a single mini transaction one at a time
   * inserts/deletes to indexes will be done in mini transactions not owned by anyone
   * updates are just logical delete (xmax = self) followed by an actual insert into the heap
   * reads will be be done without mini transactions, but in a fixed number of them, controlled by the resource usage limiter of the bufferpool
   * there will be trash_cleaner, like postgresql's vaccum
   * header will include xmin, xmax, is_xmin_committed?, is_xmax_committed?, is_xmin_aborted?, is_xmax_aborted? and both of which can be NULL
     * xmin, xmax will be as wide as transaction_table suggests
     * the *? are advisory bitmaps and should be kept updated when ever a heap page is being read (if only a read lock on page is held then let another writer do it asynchronously) or written

