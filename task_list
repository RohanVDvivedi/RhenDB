PICKED TASKS

PRIORITY 1 AGGREGATION

 * sorted_aggregation_operator -> also takes in key_element_ids and key_element_count
   * consumes tuples one by one until they have equal keys and then produced the final output when it finishes OR the unequal key tuple is found

 * hash_aggregation_operator -> also takes in key_element_ids and key_element_count
   * make rash tables with partition_count, bucket_count_per_partition, and max_worker_count
   * read tuple, clone the iterator and give it to one of the worker
   * worker reads the tuple, hashes it find out the right partition write locks it and inserts into it
   * in probe phase each worker iterates over some partitions and produces the output of the aggregation per tuple





PRIORITY 2 JOINS

 * implement streaming_loop_join_operator (identical to nested_loop_join)
   * start batching on both sides left and right
   * maintain iterators on both sides: begin, unseen_batch_first, unseen_batch_last
   * batch unseen tuples (batch_size bytes big) atleast on any side
   * and then nested loop as follows:
      for tuple1 in [begin, unseen_batch_first)
        for tuple2 in [unseen_batch_first, unseen_batch_last]
          produce(tuple1, tuple2) OR produce(tuple2, tuple1)
      for side1.batch_size = 0, start collecting the batch again for side1

 * sort_merge_join
   * iterate further the lesser side until it becomes greater than or equal to the other side
   * if both are equal consume all the equal tuples from both the sides and generate and produce their cross product
     * maintain start position of their first equal tuples
     * then iterate forward until they end
     * outer scan on the larger side and inner scan on the smaller side
   * first implementation will not use worker jobs

 * hash_join
   * make rash tables with partition_count, bucket_count_per_partition, and max_worker_count
   * read tuple, clone the iterator and give it to one of the worker
   * worker reads the tuple, hashes it find out the right partition write locks it and inserts into it
   * in probe phase each worker is given tuple from another size and is made to match with tuples on the built hash table






PRIORITY HIGH LEVEL TASKS
  * what operators to implement
    * scan on b+tree/hash index (sort rids on interim_tuple_store_sort and then scan the table), scan on heap_table(start and move ahead linearly)
    * mvcc_readable_tuple_selection_transformer should be used for the outputs of scan operator on the tables
    * joins (nested loop with per block, index nested loop, sort merge join, hash join)
    * group_by (based on hash or sorted input data)
    * read/write operators over simple data_structures (for full tuple indexer like access will be implemented later)
    * make a function to convert a sql expression from SQLtoAST into a tuple transformer

 * extended types will stay same until and as long as until their matreialized versions are required, like until they are finally needed
 * track every function call to functions in function_compare.h and function_hash.h and make sure every operator or operator-job that gets to call them are all assigned enough buffers from bufferpool of rhendb
   * like if the key_element_ids have an extended type, for external sort operator, then assign it (max_concurrent_jobs_count * 2) number of buffers
     * and that too only if the key has extended type
   * rash_table's key also ill need this
 * add logic to fail an operator if the abort_error is observed even on these compare and hash functions in these operators

 * implement expression evaluation using the expression tree from SQLtoAST

 * catalog tables
  * types
    * bitfield 1-64, int 1-32, uint 1-32, float, double, binary, string, blob, text, and rest will be custom user defined tuples
    * definition:    id, name
    * primark key: id
  * type_attributes
    * definition:    parent_id -> types, position, name, child_id -> types, array_size [0 -> variable length, 1 -> no array inside, directly compositing the element, N >= 2 -> fixed sized array]
    * primary key: parent_id, position
    * secondary key: name, parent_id, position
  * db_objects
    * definition:    id, name, db_object_type[table, btree, hash, rtree, inverted], type_id -> types, is_index_of_id -> db_objects[db_object_type = table], is_altered_from_id -> db_objects[name = name]
    * primary key: id
    * secondary key: name, id
  * indexed_attributes
    * definition:    index_id -> db_objects, position, table_positional_accessor(array of uint32_t or NULL), sql_expression(over the whole row)

FUTURE TASKS

 1*
  * expression evaluation using JIT
  * implement various types of default runtime functions
   * boolean(convert NULL and ZERO to false and rest everything is true)
   * sign(negatives to -1, 0 to 0, and positives to 1)
   * compare(for comparing any types)
   * add, sub, mul, divide, mod, gcd, lcm, pow, sin, cos, multiplicate inverse, additive inverse
   * strings and blobs to be concatenated, substring with from and to
   * unix epoch to date, day, month, year, second, millisecond, microsecond, with timezones
   * build more complex functions transformers with local variables and bytecode stored in context
   * length of string, blob, array, tuple, etc

 2.* maintain the inserts and deletes log as a stack in volatile page store and allow save points (*future)
   * savepoints will flip the is_xmin_NULL bit to 1 to undo an insert
   * and will flip the is_xmax_NULL bit to 1 to undo a delete
   * then we will do this until we hit a ROLLBACK to savepoint is hit
   * we will let vaccum do it's task of cleaning up inserts later in time
   * this stack will be in volatile page store and upon a crash we will just consider this transaction as aborted and we will never need this information

 3.* define statistics tables (*future and (low priority)) - persistent
   * same structurally and composed of indexes and heap_tables 

 4.* lock table remaining tasks (*future)
   * deadlock detection and resolution task job (*future, for now rely on timeouts to back off and abort)
   * define basic lock martix for row, heap_table (ordering_lock), simple reader writer lock (*future, do it when query optimizer and catalog tables are ready)

 5.* implement sum and avg aggregate functions (*future)
   * many more aggregate functions to be implemented

DESIGN METHODOLOGY
   * we will use postgresql architecture over (mysql like) mini transactions
   * we will have heap_table for storing tables, insert to which gives us a page_id (physical page_id) and a tuple_index (slot no) in that page
   * indexes will always include the page_id, tuple_index in the index key right after the actual key columns, and the index entry will be index key followed by the covering columns
   * inserts to table
     * insert row in heap_table
     * insert all index entries in all indexes, only after heap table row is inserted
   * true deletion only happens in vaccum job
     * scan heap_table and find the rows on a page that need to be discarded
     * discard all the index entries for these row
     * only then discard the heap_table row
   * This above ordering ensures that we will never find an index entry that has a dangling heap tuple pointer
     * i.e. on following an index entry, we will always find something
   * inserts/deletes will always be done to heap in a single mini transaction one at a time
   * inserts/deletes to indexes will be done in mini transactions not owned by anyone
   * updates are just logical delete (xmax = self) followed by an actual insert into the heap
   * reads will be be done without mini transactions, but in a fixed number of them, controlled by the resource usage limiter of the bufferpool
   * there will be vaccum, like postgresql
   * header will include xmin, xmax, is_xmin_committed?, is_xmax_committed?, is_xmin_aborted?, is_xmax_aborted? and both of which can be NULL
     * xmin, xmax will be as wide as transaction_table suggests
     * the *? are advisory bitmaps and should be kept updated when ever a heap page is being read (if only a read lock on page is held then let another writer do it asynchronously) or written

