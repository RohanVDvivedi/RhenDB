PICKED TASKS

 * implement rash_table
   * store uint64_t hash_value -> key attributes tuple_list(dti1, dti2, dti3 ...  512 B in prefix), uint64_t value_size_in_bytes, value blob(256 B in prefix)
   * there will be a global const hash_table_tuple_defs
   * there will also be a rash_table_tuple_defs
   * struct rash_table_handle {
      uint64_t root_page_id;
      uint64_t element_count; // incremented on every insert, and decremented on every remove
      uint64_t bucket_count; // incremented on every succesfull expand, and decremented on every successfull shrink, done when inserted key-value or removed one drops below a threshold
      // (shrink if crossed) 10 <= (total_inline_size / bucket_count) <= 30 (expand if crossed)
      uint64_t total_inline_size;
      tuple_defs* key_tuple_defs;
      rage_engine* volatile_engine;
    };
   * struct rash_table_key {
      const void* record;
      const tuple_def* record_def;
      const positional_accessor* key_element_ids;
      uint32_t key_element_count;

      int is_hash_value_valid;
      uint64_t hash_value;
    };
     * initialize_rash_table_key(rash_table_key* rkey_p, const void* record, const tuple_def* record_def, const positional_accessor* key_element_ids, uint3_t key_element_count)
     * get_hash_value_for_rash_table_key(rash_table_key* rkey_p)
   * rash_table_iterator {
      rash_table_handle* ;
      hash_table_iterator* ;
      rash_table_key* ;
   }
   * api will expect unique keys in the hash table, ith keys that :
    * rash_table_handle get_new_rash_table(const data_type_info** key_dtis);
    * void destroy_rash_table(rash_table_handle* rth_p);

    * rash_table_iterator* find_equals_in_rash_table(rash_table_handle* rth_p, const rash_table_key* rkey_p);

    * int exists_in_rash_table_iterator(const rash_table_iterator* rti_p);

    * int remove_from_rash_table_iterator(rash_table_iterator* rti_p);

    * binary_read_iterator* read_value_in_rash_table_iterator(rash_table_iterator* rti_p);

    * binary_write_iterator* open_for_writing_value_in_rash_table_iterator(rash_table_iterator* rti_p);
    // copies the inline content of hash table iterator OR cretes a ne one if it presently is NULL
    * void close_and_write_value_in_hash_table_iterator(rash_table_iterator* rti_p, binary_write_iterator* bwi_p);
    // closes the binary_write_iterator, inserts if the current tuple is NULL, else updates it

    * rash_table_iterator* find_all_in_rash_table_iterator(rash_table_handle* rth_p);
    * void next_in_rash_table_iterator(rash_table_iterator* rti_p); // fails if rash_table_key is found in the iterator

    * void delete_rash_table_iterator(rash_table_iterator* rti_p);
   * all the above functions come with transaction_id and abort_error, just like the tuple_indexer types

 * implement cross_join_operator, to produce cross product of tables, left and right
   * batch 16 KB of tuples atleast on either size
   * after a batch is collected, iterate on all prior seeen tuple on the other side, and produce results

 * define struct for implementing aggregate functions
   struct aggregate_function {
      void* context;
      dstring create_state(void* context);
      void* update(void* context, dstring state, void* input_tuple);
      void merge(void* context, dstring state1, const dstring state2);
      void destroy_state(void* context, dstring state);
   }

PRIORITY HIGH LEVEL TASKS
  * what operators to implement
    * scan on b+tree/hash index (sort rids (per 1 or set of buckets for hash index OR about some 1000 pages for bplus tree) and then scan the table), scan on table(start and move ahead linearly)
    * mvcc_readable_tuple_selection_transformer should be used for the outputs of scan operator on the tables
    * merge_sorted_inputs_operator to merged sorted runs from N different operators
    * joins (nested loop with per block, index nested loop, sort merge join, hash join)
    * group_by (based on hash or sorted input data)
    * read/write operators over simple data_structures (for full tuple indexer like access will be implemented later)
    * and a single sink operator that reads/writes data to a Cutlery stream tuple-by-tuple
    * similarly we will have 2 different types of source operators too, one reading from database, one reading data from csv-files, one reading data from network files, and finally one generator based like the one in the templates
  * every operator's output will/can have a filter/projection stage as it's output_tuple_transformers

 * implement expression evaluation using the expression tree from SQLtoAST

 * catalog tables
  * types
    * bitfield 1-64, int 1-32, uint 1-32, float, double, binary, string, blob, text, and rest will be custom user defined tuples
    * definition:    id, name
    * primark key: id
  * type_attributes
    * definition:    parent_id -> types, position, name, child_id -> types, array_size [0 -> variable length, 1 -> no array inside, directly compositing the element, N >= 2 -> fixed sized array]
    * primary key: parent_id, position
    * secondary key: name, parent_id, position
  * db_objects
    * definition:    id, name, db_object_type[table, btree, hash, rtree, inverted], type_id -> types, is_index_of_id -> db_objects[db_object_type = table], is_altered_from_id -> db_objects[name = name]
    * primary key: id
    * secondary key: name, id
  * indexed_attributes
    * definition:    index_id -> db_objects, position, table_positional_accessor(array of uint32_t or NULL), sql_expression(over the whole row)

FUTURE TASKS

 1*
  * expression evaluation using JIT
  * implement various types of default runtime functions
   * boolean(convert NULL and ZERO to false and rest everything is true)
   * sign(negatives to -1, 0 to 0, and positives to 1)
   * compare(for comparing any types)
   * add, sub, mul, divide, mod, gcd, lcm, pow, sin, cos, multiplicate inverse, additive inverse
   * strings and blobs to be concatenated, substring with from and to
   * unix epoch to date, day, month, year, second, millisecond, microsecond, with timezones
   * build more complex functions transformers with local variables and bytecode stored in context
   * length of string, blob, array, tuple, etc

 2.* maintain the inserts and deletes log as a stack in volatile page store and allow save points (*future)
   * savepoints will flip the is_xmin_NULL bit to 1 to undo an insert
   * and will flip the is_max_NULL bit to 1 to undo a delete
   * then we will do this until we hit a ROLLBACK to savepoint is hit
   * we will let vaccum do it's task of cleaning up inserts later in time
   * this stack will be in volatile page store and upon a crash we will just consider this transaction as aborted and we will never need this information

 3.* define statistics tables (*future and (low priority)) - persistent
   * same structurally and composed of indexes and heap_tables 

 4.* lock table remaining tasks (*future)
   * deadlock detection and resolution task job (*future, for now rely on timeouts to back off and abort)
   * define basic lock martix for row, heap_table (ordering_lock), simple reader writer lock (*future, do it when query optimizer and catalog tables are ready)

 5.* optimize sorter even more
  * 1M 65 byte tuples in 1.88 seconds (other stages of the pipeline takes 1 second) is too slow
  * 100M tuples (total 65 GB) rush through the pipeline being sorted in 4 minutes and 9 seconds

DESIGN METHODOLOGY
   * we will use postgresql architecture over (mysql like) mini transactions
   * we will have heaps for storing tables, insert to which gives us a page_id (physical page_id) and a tuple_index (slot no) in that page
   * indexes will always include the page_id, tuple_index in the index key right after the actual key columns, and the index entry will be index key followed by the covering columns
   * standard latch rules apply index->heap_page, heap_pages_tree->heap_page
   * inserts/deletes will always be done to heap in a single mini transaction one at a time
     * inserts/deletes to indexes will be done in mini transactions not owned by anyone
   * updates are just delete followed by insert into the heap
   * reads will be be done without mini transactions, but in a fixed number of them, controlled by the resource usage limiter of the bufferpool
   * there will be vaccum, like postgresql
   * header will include xmin, xmax, is_xmin_committed?, is_xmax_committed?, is_xmin_aborted?, is_xmax_aborted? and both of which can be NULL
     * xmin, xmax will be as wide as transaction_table suggests
     * the *? are advisory bitmaps and should be kept updated when ever a heap page is being read (if only a read lock on page is held then let another writer do it asynchronously) or written
   * we will never chain mini transactions for a high level transaction, we will just ensure that the transactions read only the data that is visible to them, and vaccum out invisible rows
